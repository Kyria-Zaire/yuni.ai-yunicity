NEXT_PUBLIC_YUNI_API_URL=http://127.0.0.1:8000
# Google Cloud Console → APIs & Services → Maps JavaScript API → Credentials (clé navigateur restreinte aux domaines)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
# Optionnel : Map ID (vector + Advanced Markers) — créer dans Google Cloud → Maps → Map Management
NEXT_PUBLIC_GOOGLE_MAP_ID=yuni-reims
NEXT_PUBLIC_APP_ENV=development
